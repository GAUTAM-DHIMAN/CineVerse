server:
  port: 8082

spring:
  application:
    name: movie-service
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/cineverse_movies}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
