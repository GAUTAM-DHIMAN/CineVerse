# API Design

See each service for endpoints:

- Auth: `/api/auth/register`, `/api/auth/login`, `/api/auth/profile`
- Movies: `/api/movies` CRUD and `/api/movies/search`
- Reviews: `/api/reviews` CRUD and `/api/reviews/movie/{movieId}`
