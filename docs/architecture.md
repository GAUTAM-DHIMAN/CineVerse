# Architecture

Microservices architecture using Spring Boot services behind a Spring Cloud Gateway. Datastores: PostgreSQL for structured data, MongoDB for reviews, Redis for cache, RabbitMQ for messaging.

See `docker/docker-compose.yml` for local orchestration.
